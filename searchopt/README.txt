sitemap.xml is a site map generated by Xenu on June 19, 2013.

It has URLs for items/show/####    These are not PURLS.

Instead, one should be generated that has URLs for item/[identifier]   Those are the PURLs.